plugins {
    id 'java'
}

group = 'com.lw'
version = '1.0.0'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(8)
    }
}

jar {
    archiveBaseName = 'crtcheck'
    archiveVersion = ''
    archiveClassifier = ''
    manifest { attributes 'Main-Class': 'com.lw.CrtLogRecord.CLISyntaxCheck' }
}
